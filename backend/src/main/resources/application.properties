# Let DispatcherServlet surface unmatched routes and methods to the global API error handler.
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

# Tomcat otherwise rejects TRACE before Spring MVC can translate it to the API's 405 response.
server.tomcat.allow-trace=true
